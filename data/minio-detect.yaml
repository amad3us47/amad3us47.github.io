# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: minio-detect

info:
  name: Minio panel detect
  author: pikpikcu
  severity: info
  tags: panel

http:
  - method: GET
    path:
      - "{{BaseURL}}/minio/login"
    matchers-condition: and
    matchers:
      - type: word
        words:
          - "<title>MinIO Browser</title>"
          - "<title>Minio Browser</title>"
        part: body
        condition: or
      - type: status
        status:
          - 200
