# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: perl-status-page
info:
  name: Apache mod_perl Status Page
  author: unknown
  severity: info

params:
  - root: "{{.BaseURL}}"

variables:
  - stats: |
      /
      /server-status
      /perl-status
      /stats
      /_stats

http:
  - method: GET
