# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: gradle-enterprise-panel

info:
  name: Gradle Enterprise Panel
  author: Adam Crosser
  severity: info
  tags: panel

http:
  - method: GET
    path:
      - '{{BaseURL}}/'
    redirects: true

    matchers:
      - type: word
        words:
          - '<title>Gradle Enterprise'
