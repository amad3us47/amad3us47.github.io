# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: solr-rce

info:
  name: solr命令执行
  author: str1am
  severity: high
  tags: solr

http:
  - method: GET
    path:
      - "{{BaseURL}}/solr/supplierSearch_V2/dataimport"
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        words:
          - "responseHeader"
          - "initArgs"
          - "statusMessages"
        part: body
        condition: and
