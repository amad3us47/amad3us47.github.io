# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: nginx-conf-exposed
info:
  name: Nginx Config File Exposed
  author: unknown
  severity: info

params:
  - root: "{{.BaseURL}}"

variables:
  - stats: |
      nginx.conf
      static/nginx.conf

http:
  - method: GET
