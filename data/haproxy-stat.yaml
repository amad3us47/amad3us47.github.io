# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: haproxy-stat
info:
  name: HA Proxy Statistics
  author: unknown
  severity: info

params:
  - root: "{{.BaseURL}}"

variables:
  - stats: |
      /stats
      /_stats
      /haproxy_stats
      /;norefresh

http:
  - method: GET
