# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: rabbitmq-dashboard
info:
  name: RabbitMQ Dashboard
  author: fyoorer
  severity: info
http:
  - method: GET
    path:
      - '{{BaseURL}}'
    matchers:
      - type: word
        words:
          - "RabbitMQ Management"
        part: body
