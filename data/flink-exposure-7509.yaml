# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: flink-exposure

info:
  name: Apache Flink Exposure
  author: pdteam
  severity: low
  tags: panel

http:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers:
      - type: word
        words:
          - '<title>Apache Flink Web Dashboard</title>'
