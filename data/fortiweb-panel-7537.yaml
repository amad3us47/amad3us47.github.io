# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: fortiweb-login

info:
  name: Fortinet FortiWeb Login Panel
  author: PR3R00T
  severity: info
  tags: panel

http:
  - method: GET
    path:
      - "{{BaseURL}}/login"
    matchers-condition: and
    matchers:
      - type: word
        words:
          - "Please login"
          - "ftnt-fortinet-grid"
          - "main-fortiweb.css"
        condition: and
        part: body

      - type: status
        status:
          - 200
