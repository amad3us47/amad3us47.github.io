# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: werkzeug-debug

info:
  name: werkzeug-debug
  author: notnotnotveg
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}/console"
    matchers:
      - type: word
        words:
          - "Werkzeug"
        part: body
