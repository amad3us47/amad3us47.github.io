# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: nginx-vhosts-stat
info:
  name: Nginx Vhost Stats
  author: unknown
  severity: info

params:
  - root: "{{.BaseURL}}"

variables:
  - stats: |
      /
      /server-status
      /status.html
      /stats
      /_stats

http:
  - method: GET
