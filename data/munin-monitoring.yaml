# yaml-language-server: $schema=https://raw.githubusercontent.com/projectdiscovery/nuclei/dev/pkg/templates/nuclei-jsonschema.json
id: munin-monitoring

info:
  name: Munin Monitoring
  author: notnotnotveg
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}/munin/localhost/"
    matchers:
      - type: word
        words:
          - "<title>Munin"
        part: body
